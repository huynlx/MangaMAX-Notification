import { FC } from 'react';

const Header: FC = () => {
  return (
    <div className='flex justify-center'>
      <div className='h-16 w-full flex items-center justify-between max-w-[1000px] mx-3'>
        Notification
      </div>
    </div>
  );
};

export default Header;
